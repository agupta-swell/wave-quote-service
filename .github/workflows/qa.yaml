name: Deploy
run-name: Deploy 
on:
  push:
    branches:
      - qa
  workflow_dispatch:
jobs: 
  deploy_qa:
    uses: SwellEnergy/actions/.github/workflows/backend.yaml@development
    with:
      Environment:      QA
      SWL_PROCESS_ID:   wave-quote-service
    permissions:
      id-token: write
      contents: read